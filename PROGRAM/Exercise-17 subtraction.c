#include <stdio.h>
void main(){
  float a,b,c;
  printf("give first number:");
  scanf("%f",&a);
  printf("give second number:");
  scanf("%f",&b);
  if (a>b) c=a-b;
  else if (c=b-a);
  printf("the difference between the two numbers:%f",c);
  }
